This is an example of integrating an attack (taken from the web) into the platform

The scripts was taken as is, This lines were added instead of user input:

parameters = core_get_parameters()
IP = parameters['ip']
Port = parameters['port']

And the platform can run this Group.

comment: in order to use the DoS attack as testing tool more code is required.